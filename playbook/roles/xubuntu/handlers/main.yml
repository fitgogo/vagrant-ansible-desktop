---
- name: reboot server
  command: reboot
  async: 0
  poll: 0
  ignore_errors: true

- name: wait for reboot
  local_action:
    module: wait_for
      host="{{ inventory_hostname }}"
      port=22
      delay=1
      timeout=300

